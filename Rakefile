#!/usr/bin/env rack
# Add your own tasks in files placed in lib/tasks ending in .rack,
# for example lib/tasks/capistrano.rack, and they will automatically be available to Rake.

require File.expand_path('../config/application', __FILE__)

Ba1Rails::Application.load_tasks
